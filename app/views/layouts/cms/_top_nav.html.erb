<% nav_menu = @group && @group.admin_nav ? @group.admin_nav : NavMenu.admin_nav %>

<% if nav_menu %>
	<nav>
		<% nav_menu.nav_items.each do |nav_item| %>
			<%= admin_nav_link(nav_item) %>
		<% end %>
	</nav>	
<% end %>